# Handoff：像素風疊房子遊戲（設計方向 2a「日光工地」）

## Overview
一款類 Tower Bloxx 的疊房子街機遊戲：畫面上方吊車掛勾左右擺動，玩家點擊／按空白鍵放下方塊，方塊受重力落下疊到塔上；對齊精準度決定 Perfect 加分或大樓晃動，偏差過大則方塊滑落扣命。本包為視覺設計 2a「日光工地像素風」的完整交接文件。

## About the Design Files
本包內的 `疊房子遊戲 設計稿.dc.html`（搭配 `support.js` 開啟）是 **HTML 製作的設計參考稿**，展示預期的外觀與行為，**不是可直接沿用的產品程式碼**。任務是在目標程式環境中**重新實作**這些設計 —— 建議使用 **HTML5 Canvas + 原生 JavaScript**（單一 index.html 即可執行），若專案已有既定框架（React / Phaser / Unity 等）則沿用其慣例。
設計稿共兩輪：**只參考最上方的 turn 2（2a）**；turn 1（1a/1b）為已淘汰的早期方向。`uploads/` 內三張截圖為使用者提供的風格參考來源。

## Fidelity
**High-fidelity**：配色、方塊構造、場景元素、UI 佈局皆為定案，請依下列 token 精確重現。像素風請全程使用硬邊緣（無抗鋸齒、無圓角柔和陰影；`image-rendering: pixelated` 概念）。

## 遊戲畫面（Screens）

### 1. 主選單
- 背景：淺灰天空 `#d2d6d8`，左右灰色高樓剪影（`#b8bcbe`、橫紋樓 `repeating #a2a8ab/#b4babd 22px`），一棟藍玻璃直條樓（`#8fc0e4`/`#c6e4f6` 直條 9px + 灰 12px）
- 地面：泥土 `#a06c30`，散落深色土塊 `#7c5222`
- 左側鐵網圍籬（灰 `#c8ccce` 底 + 45°/-45° 交叉線 `rgba(90,96,100,.55)`，頂欄 `#6a7074`）；右側木板圍籬（`#d9b667` 板 14px + 縫 `#c39c4e` 3px，頂欄 `#a87f34`）
- 綠樹（兩色像素簇 `#3f8030`/`#56a340`）、橘白三角錐（`#e06a28`/白）
- 中央：混凝土平台（面 `#cdcfd1`、身 `#aeb0b2`、底 `#838587`）上放一個地基方塊（含玻璃門）
- 標題「疊疊大樓」42px 深藍 `#2c3e50` 配白色像素描邊；副標 CITY BLOXX（Press Start 2P 8px `#b87c14`）
- 按鈕「開始遊戲」：黃 `#e8a428`、字 `#3a2a08`、邊框 3px `#8a5a10`、下方 4px 硬陰影 `#8a5a10`
- 次要「♪ 音效：開」：白底、灰邊 `#6a7074`

### 2. 遊戲中（低樓層／工地）
- 同上工地場景；吊索黑 `#1c1c1c`（寬 5px）自畫面頂垂下、以頂端為軸擺動（設計稿示意 ±13°）
- 吊掛方塊隨擺動傾斜（示意 -12°），頂部中央有金色吊環 `#d8a828`
- 白色弧線（75% 不透明）暗示掉落軌跡
- HUD：右上金幣（金圓 `#f2b830` 邊 `#b87c14`、$ 符號 + 數字，文字 `#3a4148` 配 1px 白陰影）；左下樓層計數（黑底 `#2c2c2c` 圓角 4px、黃字 `#f2c218`，如「001 F」）；底部中央提示「點擊 放下方塊」

### 3. 遊戲中（高空 + Perfect）
- 背景改為滿版高樓群（藍玻璃直條樓 + 灰樓交錯），代表鏡頭已上移
- 塔由畫面底部往上堆，各層有 ±4–6px 水平偏移表示非完美疊放
- 最新一層 Perfect：邊框改亮黃 `#f2c218` + 微光暈，周圍散金色星星（`#f2c218`/`#ffd84a` 7px 方點）
- 「PERFECT!」Press Start 2P 19px `#f2c218`，陰影 `3px 3px 0 #8a5a10` + 白描邊；下行「+300　連擊 ×7」
- HUD 追加：頂部連擊進度條（槽 `#9aa0a3` 邊 `#6a7074`、填充 `#f2c218`、右側 ×7）；右上「+17」飄字 + 金幣總數；左下樓層計數上方疊小黃塔圖示；右下計時 0:42（白字深灰陰影）
- 彩蛋：小人撐傘飄落（藍傘 + 黑色小人，見設計稿）

### 4. Game Over
- 背景疊半透明深灰遮罩 `rgba(44,52,60,.6)`，後方塔傾斜 7°
- 對話框：淺灰 `#e6e8ea`、邊框 4px `#6a7074`、下方 6px 硬陰影 `#4a5054`
- 標題列：黃底 `#e8a428`、底線 4px `#8a5a10`、「遊戲結束」24px `#3a2a08`
- 數據列（14px `#3a4148`，數值金色 `#c98a10`）：最終得分／最高連擊／樓層高度／獲得金幣
- 按鈕「重新開始」同主選單黃按鈕；下方文字連結「回主選單」

### 5. 桌機版（1024×576 橫式）
- 同高空場景放寬；連擊條在頂部拉長；底部中央提示「按下 [空白鍵] 放下方塊」（按鍵用白底灰邊膠囊）
- 手機直式 288×512（設計稿比例，實作請用 9:16 滿版）

## 方塊構造（核心美術，務必精確）
正方形房間方塊（手機尺度 78×78、地基 82×82／86×86、桌機 90×90；實作可統一一種尺寸按比例縮放）：
- 本體：橫向明暗漸層 `linear-gradient(90deg, #f6bc48 0 9px, #eaa82c 9px 68%, #d98f1a 68% 88%, #b87814 88% 100%)`（左亮右暗的立體感）
- 外框：3px `#8a5a10`；底部內陰影 `inset 0 -7px 0 rgba(122,74,8,.4)`
- 頂蓋：銀灰金屬條（高 6–7px，`#eceeef → #b4b8ba` 縱向漸層）
- 窗戶 ×2：深藍 `#24486a`、銀框 2px `#c4c8ca`、內部左右兩片玻璃 `#8ec2e8` / `#a9d4f2`
- 地基方塊：單一大玻璃門（同窗戶配色，3px 銀框）
- 吊環：金色 `#d8a828` 圓、邊 2px `#8a5a10`，置於頂部中央
- 吊鉤：金色 `#d8a828` 開口向上的鉤形；吊索黑 `#1c1c1c` 5px

## Interactions & Behavior（依四階段開發）
**階段一（核心）**：掛勾以鐘擺方式擺動（角度 = maxAngle × sin(t×speed)，吊掛方塊位置隨之傾斜）；點擊／空白鍵放下 → 方塊脫離、垂直受重力（v += g）下落；碰到地基或塔頂即吸附固定成新一層。
**階段二（判定與晃動）**：中心偏差 ≤5px → Perfect 特效 + 加成；有重疊但未對齊 → 依偏差比例讓整棟塔左右晃動（以塔底為軸的阻尼簡諧擺，角度∝累積偏差）；偏差 > 方塊寬 50% → 方塊滑落出畫面，扣 1 命。
**階段三（鏡頭）**：塔高超過畫面 1/2 → 鏡頭平滑上移（lerp），最新層保持畫面中下；掛勾生成高度同步上移；畫面下方過遠的固定方塊只保留資料、停止繪製／物理（或合併為單一貼圖）以省記憶體。
**階段四（流程與 UI）**：每層 +100 分，連續 Perfect 倍率 ×1/×2/×3…；3 條命；HUD 顯示分數、最高連擊、生命（本設計以金幣/樓層計數呈現，可並用）；命歸零 → 暫停 + Game Over 視窗（最終得分 + 重新開始）。
- 過場：Perfect 星星粒子 0.5s 淡出；「+N」飄字上移淡出 0.8s；鏡頭移動 ease-out ~0.6s。

## State Management
- `gameState`: menu / playing / gameover
- `craneAngle, craneT`（擺動）、`hangingBlock`、`fallingBlock {x, y, vy}`
- `tower: [{x, width}]`（各層水平位置）、`swayAngle, swayVelocity`（塔晃動）
- `score, combo, maxCombo, coins, lives, floorCount, cameraY`

## Design Tokens 總表
- 天空/背景：`#d2d6d8`、`#cdd2d4`；灰樓 `#b8bcbe` `#a8adb0` `#b4babd` `#a2a8ab` `#babfc2`；藍玻璃 `#8fc0e4` `#c6e4f6` `#e6eaec`
- 泥土 `#a06c30`／`#7c5222`；平台 `#cdcfd1` `#aeb0b2` `#838587`
- 方塊黃：`#f6bc48` `#eaa82c` `#d98f1a` `#b87814`，框 `#8a5a10`，亮黃 `#f2c218` `#ffd84a`
- 窗：`#24486a` `#16324a`，玻璃 `#8ec2e8` `#a9d4f2`，銀框 `#c4c8ca`
- 吊具：索 `#1c1c1c`、鉤/環 `#d8a828`
- UI：文字 `#3a4148`、金幣 `#f2b830`/`#b87c14`、金額字 `#c98a10`、計數底 `#2c2c2c`、對話框 `#e6e8ea`/`#6a7074`/`#4a5054`
- 圍籬：`#c8ccce` `#6a7074`（鐵網）、`#d9b667` `#c39c4e` `#a87f34`（木板）；樹 `#3f8030` `#56a340`；三角錐 `#e06a28`
- 字體：介面中文 **Cubic 11**（Google Fonts，像素中文字型；備援 DotGothic16）；英文特效字 **Press Start 2P**
- 邊框/陰影慣例：3px 實心深色邊框 + 垂直硬陰影（`0 4px 0`），全部直角、無模糊

## Assets
無外部圖片；所有美術皆以純 CSS/Canvas 幾何構成。`uploads/` 三張 PNG 為風格參考截圖（來自使用者提供的影片畫面），僅供比對，勿直接嵌入產品。

## Files
- `疊房子遊戲 設計稿.dc.html` — 設計稿本體（turn 2 = 2a 為定案；turn 1 已淘汰）
- `support.js` — 設計稿的執行環境（與 dc.html 放同資料夾即可在瀏覽器開啟）
- `uploads/*.png` — 風格參考截圖
- `screenshots/01~05-screen.png` — 2a 各畫面成品截圖（依序：主選單／遊戲中工地／高空 Perfect／Game Over／桌機版，桌機版為 75% 縮放）
